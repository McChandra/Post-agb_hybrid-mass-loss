# Regenerates notebook.ipynb.  See docs/report.md for the rendered walkthrough.
# (The executed notebook is the canonical deliverable; this script is provided
#  so the notebook can be rebuilt if the modules are edited.)
print("Use: jupyter nbconvert --to notebook --execute --inplace notebook.ipynb")
